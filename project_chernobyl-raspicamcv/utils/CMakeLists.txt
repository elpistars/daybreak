INCLUDE_DIRECTORIES(${PROJECT_SOURCE_DIR}/src)
LINK_LIBRARIES(${PROJECT_NAME} ${REQUIRED_LIBRARIES})

 
IF (OpenCV_FOUND)
ADD_EXECUTABLE(pw pw.cpp )
target_link_libraries(pw  ${PROJECT_NAME}_cv)
INSTALL(TARGETS pw   RUNTIME DESTINATION bin)

ENDIF()
