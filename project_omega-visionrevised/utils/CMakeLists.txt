INCLUDE_DIRECTORIES(${PROJECT_SOURCE_DIR}/src)
LINK_LIBRARIES(${PROJECT_NAME} ${REQUIRED_LIBRARIES})

IF (OpenCV_FOUND)
ADD_EXECUTABLE(ELP elpmain.cpp )
target_link_libraries(ELP  ${PROJECT_NAME}_cv -ldxl -lm -lpthread)
INSTALL(TARGETS ELP   RUNTIME DESTINATION bin)

ENDIF()
